Nicolás just received a new training routine assigned by his coach for period {period}. His goal is {goal}.

## New routine structure

{routine}

## Full training history (compact — settled weights per period)

{history}

## Pesos de cierre por ejercicio (pre-calculados — usar como baseline directo)

{settled_weights_block}

---

## REGLAS DE OPERACIÓN — LEER ANTES DE ESCRIBIR CUALQUIER COSA

**REGLA 1 — PROHIBICIÓN DE RUIDO**
Cada afirmación que escribas debe estar respaldada por un dato numérico explícito extraído del historial de arriba. Si no encontrás el número, no escribís la frase. Prohibido:
- Rangos genéricos ("2.5 a 5 kg", "90 a 120 segundos") sin citar la fecha o semana donde Nicolás los usó.
- Consejos de gimnasio generales ("comé proteína", "dormí bien", "escuchá tu cuerpo").
- Repetir en prosa lo que ya está en el JSON estructurado (especialmente tiempos de descanso — ya están en `rest_s`). La prosa solo justifica anomalías o lógica mecánica no-obvia.

**REGLA 2 — CAPA DE SEGURIDAD (RIR)**
El siguiente script detectó automáticamente los ejercicios de esta rutina con carga axial en barra libre:

EJERCICIOS CON CARGA AXIAL: {axial_load_exercises}

Para TODOS los ejercicios en esa lista:
- La instrucción SIEMPRE es mantener RIR 1-2 (1-2 repeticiones en reserva). NUNCA RIR 0 ni fallo concéntrico.
- Está TERMINANTEMENTE PROHIBIDO sugerir drop sets, rest-pause o forzadas en esos ejercicios, aunque el historial los muestre. Si aparecen en el historial, marcarlos como práctica de riesgo, no como algo a continuar.

El RIR 0 (fallo real en la última serie) SOLO se puede prescribir en ejercicios de aislamiento, poleas o máquinas guiadas (Prensa Hammer, poleas, extensiones en máquina).

**REGLA 3 — DROP SETS: PRESCRIPCIÓN QUIRÚRGICA**
No des advertencias vacías de peligro. Si vas a prescribir un drop set, que sea concreto:
- Únicamente en la **última serie de la última semana** del bloque (semana 4).
- Únicamente en ejercicios donde el historial muestre que Nicolás ya tiene tolerancia técnica a la alta densidad (poleas, Hammer, máquinas guiadas).
- Especificá los pesos exactos del drop basándote en los que ya usó.
Si no hay evidencia de tolerancia técnica en el historial → no prescribís el drop set.

---

Write two sections in Spanish:

### Cómo usar esta rutina para crecer

**1. Progresión de pesos: qué dice su historial**
For each exercise in this new routine, look up its actual week-by-week progression in the most recent period and report: W1→W2→W3→W4 weights, whether it was linear/stalled/regressed, and the actual jump size he used. Based on that pattern, give ONE concrete recommendation for this new period (e.g. "arrancá 2.5 kg por debajo del cierre anterior para tener 4 semanas de margen").
If an exercise has no history → say so, state which reference you're using, and recommend starting at RIR 2 conservador.
Do NOT write any progression logic that isn't directly traceable to a specific data point in the history above.

**2. Drop sets: historial y prescripción**
Scan the history for drop sets (same exercise, same week, decreasing weights across series). For each one found:
- Quote the exact weights (period + week).
- Note if the exercise is now combined or isolated in this new routine, and adjust accordingly.
- Apply Regla 3: if eligible (cable/machine, history shows tolerance) → prescribe the drop for week 4, last set, with exact weights. If not eligible → do not prescribe it.

**3. Margen vs. fallo: ejercicio por ejercicio**
For each exercise, check the last week of the most recent period: did the series weights stay flat (margin) or drop (near failure)? Report it concretely per exercise with actual numbers. Apply Regla 2 for axial load exercises — always conclude with RIR 1-2, regardless of what the data shows.

**4. Volumen y frecuencia: qué dicen los números reales**
The following volume was calculated directly from the coach's routine by the script — exact numbers, not estimates:

{volume_block}

State whether each muscle group is sufficient for hypertrophy (~10–20 direct sets/week). Flag anything below 6 direct sets/week as critically low, paying special attention to muscles relevant to {goal}.
If a muscle is low, suggest ONE fix: an existing exercise that could add a set, on a day that already trains that muscle as primary. If fixing it would require violating the split → say so and leave the call to the coach.
**STRICTLY ENFORCED**: Never suggest adding exercises from one muscle group to a day dedicated to a different group.

### Pesos sugeridos
After the analysis, output a JSON block with suggested starting weights for each exercise. Use the pre-calculated baseline from **"Pesos de cierre por ejercicio"** above as the primary source — do NOT derive weights from the compact history text. Priority:
1. If the exercise has a baseline in the settled weights block → use that weight directly, adjusted only for combined/isolated context change.
2. If the exercise has no prior history → estimate from the most recent history of exercises with the same movement pattern and muscle group, and state that explicitly in "reason".

When the exercise appears as isolated in history but is now combined (or vice versa), adjust accordingly: combined execution limits output due to accumulated fatigue — expect 10–20% less than isolated baseline. If it was combined before and is now isolated, the inverse applies.

The block MUST be valid JSON inside ```json ``` fences. Use this exact format:

```json
[
  {{
    "exercise": "Nombre exacto del ejercicio",
    "day": 1,
    "weeks": [70, 72.5, 75, 77.5],
    "rest_s": 90,
    "reason": "Una línea explicando por qué este peso, basado en su historial"
  }}
]
```

Rules:
- **EXERCISE NAME**: The value of "exercise" MUST be identical, character by character, to the name provided in the routine structure above. Do not shorten, remove parentheses, or alter it in any way.
- **Include EVERY exercise** in the JSON — no exceptions. If the routine has 9 exercises in a day, the JSON must have 9 entries for that day. Never truncate or summarize.
- "weeks" must have exactly 4 values (one per week), showing progressive overload.
- For bodyweight exercises, use 0.
- "reason" must reference the **settled weight** from his history (last week of the period, not the peak) and explain any adjustment made for movement pattern or combined/isolated context.
- **rest_s format — STRICTLY ENFORCED**: `rest_s` must be a plain integer (e.g. 90, 120) or the JSON literal `null` (no quotes, no text, no dashes). Strings like `"—"`, `"null"`, `"0"`, or any non-integer value are invalid and will break the parser.
- **Combined exercises and rest:** when two or more exercises appear together as a combined set (superset), the rest time applies after the LAST exercise of the group, not between them. Set `rest_s` only on the last exercise of each combined group; set `rest_s: null` for all others in the same group.
- **Include the abdomen group in every day.** The abdomen (Abdominal recto largo, Rotaciones de pie con disco, Extensión de cadera en banco) appears as a combined group at the start of every training day. You MUST include all three in the JSON for every day, with `rest_s` only on Extensión de cadera en banco (the last of the group).
- **Abdomen weights are identical across all 4 days.** Since the abdomen group repeats the same exercises every day, the "weeks" array must be exactly the same for each exercise regardless of which day it appears in. Do NOT vary abdomen weights between days.
