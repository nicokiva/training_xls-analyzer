Nicolás just received a new training routine assigned by his coach for period {period}. His goal is {goal}.

## New routine structure

{routine}

## Full training history

{history}

Write two sections:

### Cómo usar esta rutina para crecer
Write in Spanish. Be specific and data-driven — every claim must cite something from his actual history. If you can't back it up with a number or a pattern from the data, don't say it. No generic gym advice.

Structure it as follows:

**1. Progresión de pesos: qué dice su historial**
For each exercise in this new routine, look up its progression across the most recent period in {history} and report:
- The actual week-by-week weights for that exercise (W1→W2→W3→W4). If it appears as combined vs isolated, note it.
- Whether the progression was linear, stalled, or regressed in the last period.
- The actual week-over-week jump he used (e.g. "subió 2.5 kg por semana" or "se estancó en S3").
- A concrete recommendation for this new period based on that pattern (e.g. "arrancá 2.5 kg por debajo del S4 anterior para dejar margen de progresión en las 4 semanas").
If an exercise has no history, say so explicitly and explain what reference you're using for the estimate.
Do NOT give a generic rule like "si completás todos los reps, subí 2.5 kg". Use his actual data.

**2. Drop sets: qué hace y qué debería hacer**
Scan the history for drop sets — defined as: same exercise, same week, the series show decreasing weights (e.g. series 1: 30 kg, series 2: 25 kg, series 3: 20 kg). For each exercise where you find this pattern:
- Quote the actual weights (period and week where it appears).
- Say whether the exercise is now combined or isolated in this new routine, and adjust the recommendation accordingly.
- Give a specific instruction: continue, extend (add a drop), or simplify — with the actual weights to use in week 1.

**Safety rule — STRICTLY ENFORCED**: Never suggest drop sets, rest-pause, or failure techniques on free-barbell exercises under axial load: Sentadilla, Peso muerto and its variations, Empuje de hombros con barra, Empuje de pecho con barra. If you find drop sets on those exercises in his history, flag it as risky and do NOT recommend continuing. Drop sets and failure techniques are only appropriate for guided machines (Prensa, Hammer, poleas) and isolation exercises.

**3. Margen vs. fallo: ejercicio por ejercicio**
For each exercise, look at the last week of the most recent period (the settled weight). Determine whether he had obvious margin or was already near failure by checking:
- Did the series weights stay flat across all 3 series, OR did they drop (indicating he couldn't maintain)?
- Was there a drop set on the last series?
Report this concretely per exercise: "En X, las 3 series fueron a Y kg sin drops → tenía margen. En Z, la serie 3 bajó a W kg → ya estaba cerca del fallo."
Do NOT write a generic instruction to "go to failure on every last set." Only say what the data shows.

**4. Volumen y frecuencia: qué dicen los números reales**
The following volume was calculated directly from the coach's routine by the script — these are exact numbers, not estimates. Use them as-is:

{volume_block}

For each muscle group, state whether the volume is sufficient for hypertrophy (evidence: ~10–20 direct sets/week for intermediates). Flag any muscle below 6 direct sets/week as critically low. Pay special attention to muscles directly relevant to his goal ({goal}).

If a muscle is low, suggest ONE concrete fix: an exercise already in the routine that could add a set, and which day. Respect the training split — only add to days that already train that muscle as primary. If that's impossible without breaking the split, say so explicitly and leave the decision to the coach.

**Split respect rule — STRICTLY ENFORCED**: Never suggest adding exercises from one muscle group to a day dedicated to a different group (e.g. no shoulder work on a legs day).

Do NOT: give generic advice ("comé proteína", "dormí bien"), pad with positives, or repeat information already visible in the weights table.

### Pesos sugeridos
After the analysis, output a JSON block with suggested starting weights for each exercise. Base suggestions on his full history using this priority:
1. If the exact exercise appears in the immediately prior period → use that settled weight as the baseline.
2. If the exercise appears only in older periods → take that weight but adjust upward based on the progress he made in exercises with the same movement pattern during the intermediate periods.
3. If the exercise never appeared → estimate from the most recent history of exercises with the same movement pattern and muscle group.

When the exercise appears as isolated in history but is now combined (or vice versa), adjust accordingly: combined execution limits output due to accumulated fatigue — expect 10–20% less than isolated baseline. If it was combined before and is now isolated, the inverse applies.

The block MUST be valid JSON inside ```json ``` fences. Use this exact format:

```json
[
  {{
    "exercise": "Nombre exacto del ejercicio",
    "day": 1,
    "weeks": [70, 72.5, 75, 77.5],
    "rest_s": 90,
    "reason": "Una línea explicando por qué este peso, basado en su historial"
  }}
]
```

Rules:
- **EXERCISE NAME**: The value of "exercise" MUST be identical, character by character, to the name provided in the routine structure above. Do not shorten, remove parentheses, or alter it in any way.
- **Include EVERY exercise** in the JSON — no exceptions. If the routine has 9 exercises in a day, the JSON must have 9 entries for that day. Never truncate or summarize.
- "weeks" must have exactly 4 values (one per week), showing progressive overload.
- For bodyweight exercises, use 0.
- "reason" must reference the **settled weight** from his history (last week of the period, not the peak) and explain any adjustment made for movement pattern or combined/isolated context.
- **rest_s format — STRICTLY ENFORCED**: `rest_s` must be a plain integer (e.g. 90, 120) or the JSON literal `null` (no quotes, no text, no dashes). Strings like `"—"`, `"null"`, `"0"`, or any non-integer value are invalid and will break the parser.
- **Combined exercises and rest:** when two or more exercises appear together as a combined set (superset), the rest time applies after the LAST exercise of the group, not between them. Set `rest_s` only on the last exercise of each combined group; set `rest_s: null` for all others in the same group.
- **Include the abdomen group in every day.** The abdomen (Abdominal recto largo, Rotaciones de pie con disco, Extensión de cadera en banco) appears as a combined group at the start of every training day. You MUST include all three in the JSON for every day, with `rest_s` only on Extensión de cadera en banco (the last of the group).
- **Abdomen weights are identical across all 4 days.** Since the abdomen group repeats the same exercises every day, the "weeks" array must be exactly the same for each exercise regardless of which day it appears in. Do NOT vary abdomen weights between days.
